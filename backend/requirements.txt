# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1

# Validation / config
pydantic==2.9.2
pydantic-settings==2.6.1

# Database
SQLAlchemy[asyncio]==2.0.36
psycopg[binary]==3.2.3
asyncpg==0.30.0
pgvector==0.3.6
alembic==1.14.0

# HTTP client (used by the GitHub ingestion layer in Phase 2+)
httpx==0.27.2

# Utilities
python-dotenv==1.0.1
orjson==3.10.11
tenacity==9.0.0
numpy==2.1.3
PyJWT==2.10.1
pwdlib[argon2]==0.2.1

# LLM / embedding providers
# Phase 1 calls providers over HTTP via httpx (already a dependency),
# so we don't import heavy SDKs at runtime. The application code is
# provider-agnostic — choose openai, ollama, etc. via .env.
