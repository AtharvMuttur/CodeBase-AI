"""Service layer.

Each module under `services/` encapsulates a piece of business
logic: ingestion, chunking, embedding, retrieval, reranking,
LLM, eval, etc. Routers stay thin and call into these.
"""
