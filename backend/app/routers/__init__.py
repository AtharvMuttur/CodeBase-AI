"""FastAPI routers package.

Each router lives in its own file and is registered in `main.py`.
Phase 1 only ships the health check; later phases add
`repositories`, `chat`, `conversations`, `eval`, etc.
"""
